import styled from "styled-components"

const BaseHeading = styled.div``

export const Description = styled(BaseHeading)``
export const Terms = styled(BaseHeading)``
export const Label = styled(BaseHeading)``
